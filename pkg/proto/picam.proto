syntax = "proto3";

package rpi;

message RequestImage {
    int32 width = 1;
    int32 height = 2;
}

message ResponseImage {
    bytes imageBytes = 1;
}

service PiCamService {
    rpc GetFrames (RequestImage) returns (stream ResponseImage){};
}